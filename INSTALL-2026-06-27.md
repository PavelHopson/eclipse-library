# Установки 23–27.06.2026 — план внедрения по проектам

> Снапшот: что из батча 23–27.06 куда внедряем. Статусы: 🎯 high-value ·
> 📋 очередь · 📚 reference · ⏸ не внедряем. **`star-crm` / `starmarket` не трогаем**
> до одобрения Pavel. Пиратка (Bookracy, Streambert) и дубль (Qwythos) — в Grey,
> не внедряем.

## 🎯 Сделать в первую очередь

### agent-skills (tech-leads-club) → все репо · security
Verified-набор SKILL.md (Snyk + static analysis + ручная курация). Прямой ответ на
риск «вирусы в SKILL.md» (>13% маркетплейс-скиллов с критич. уязвимостями).
```bash
npx @tech-leads-club/agent-skills        # интерактивный визард
```
- Заменить/дополнить случайные community-скиллы verified-набором.
- Связка с нашим правилом «ui-skills.com ставить по одному с ревью» — тут ревью уже сделан.

### AI Stupid Level → StarCRM · FinFlow · Hub (бесплатно, сразу)
«Тупометр» моделей в реальном времени — мониторинг деградации провайдеров.
- **StarCRM:** свериться при настройке порядка `AI_PROVIDER=auto` (какой free-провайдер сейчас топ).
- **FinFlow:** какой провайдер советовать для AI-советника (Groq vs Gemini по качеству).
- **Hub:** подбор модели под модули. Закладка, не интеграция кода.

## 📋 Очередь (по репо)

### PixelRAG → StarMarket · StarCRM (парсинг)
Скриншот-RAG вместо HTML-парсинга — устойчив к анти-боту/ломаной вёрстке.
```bash
pip install pixelrag      # + плагин Claude Code "pixelbrowse"
```
- **StarMarket:** парсинг карточек WB/Ozon/ЯМ, когда HTML/анти-бот ломает текст-парсер.
- **StarCRM card-audit:** WB-карточки при таймаутах баскетов (был реальный инцидент).
- ⚠️ Оба — другой чат/одобрение; пока **reference + POC** на одной карточке.

### Baidu Unlimited-OCR → FinFlow (будущая фича) · StarCRM
OCR целого документа за проход (DeepSeek-OCR), MIT.
- **FinFlow:** скан чека → текст → авто-операция (когда дойдём до attachments).
- **StarCRM:** парсинг документов/счетов. Reference-цель, не сейчас.

### Cua → QA/SDET (EC · FinFlow)
Computer-use агент: прогон сценариев в реальном app по скриншоту, CLI + MCP.
- Кандидат на **device-QA**: автоматизировать то, что сейчас Pavel чекает руками на телефоне.
- ⚠️ Computer-control = security-чувствительно: только в изолированной среде/VM.

### Cua / ARGithub → онбординг репо
`github`→`argithub` в URL = ИИ разберёт код перед форком (мы часто форкаем).

## 📚 Reference / обучение (в библиотеке, код не внедряем)
- **Open Culture (1700 курсов)** + **Free-Certifications** → источники для Educator-AI.
- **6 промптов для языка** (`prompts/language-learning-6.md`) → пресет «Репетитор» Educator-AI.
- **Приём «Gemini делает курсы»** → референс генератора курсов Educator-AI.
- **SimpleX Chat** → архитектурный референс privacy (no-id / no-metadata) для Eclipse Chat.
- **Гайд камеры iPhone** (`guides/iphone-camera-content.md`) → кадры для Shotforge/соцсетей.
- **pytube-downloader** → референс фич (8K/плейлисты) для Eclipse Media.

## ⏸ Не внедряем (grey / пиратка / hype)
- **Sakana Fugu Ultra** — не модель, а оркестратор Gemini/ChatGPT/Claude; жжёт токены, reference.
- **Qwythos-9B** — дубль (Grey 21.06), фейк-«Claude», uncensored, вне продукта.
- **Bookracy / Streambert** — пиратка (книги/стриминг), правовой риск, не интегрируем.
- **pytube** скачивание YouTube — серая зона ToS, только личное использование.

## Следующие шаги
1. agent-skills (tech-leads-club) — поставить, заменить случайные скиллы (низкий риск).
2. AI Stupid Level — закладка, свериться при настройке AI-цепочек StarCRM/FinFlow.
3. PixelRAG / Baidu OCR — POC на одной карточке/чеке; `star-crm`/`starmarket` — по одобрению.
4. Educator-AI — собрать пресеты из промптов/курсов следующим заходом.
