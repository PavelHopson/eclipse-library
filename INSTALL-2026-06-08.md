# Установки 08.06.2026 — что встало в проекты

> Снапшот внедрения находок батча 08.06. Статусы: ✅ сделано (в PR) ·
> 🔑 нужен GPU/ключ · 📋 очередь. **`star-crm` не трогаем** до одобрения.

## ✅ Сделано в эту сессию (в PR)

### Jina Reader → Eclipse AI Hub (RAG)
**PR:** [PavelHopson/eclipse-ai-hub#4](https://github.com/PavelHopson/eclipse-ai-hub/pull/4) (ветка `feat/rag-jina-url`)
- **Где:** `src/services/ragService.ts` + `src/pages/RAG.tsx` + `src/services/ragService.test.ts`
- `parseUrl(url)`: `https://r.jina.ai/<url>` → Markdown → чанки → `RAGDocument` (зеркало `parseDocument`)
- UI: инпут «Загрузить из URL» рядом с upload файлов, тот же `ingest`-флоу
- Free-tier без ключа (~20 req/min); `VITE_JINA_API_KEY` опц. для лимитов 200/min + 1M токенов + proxy
- 3 теста (vitest): успех+чанкинг, reject non-http, friendly-error на non-ok Jina
- Источник: https://github.com/jina-ai/reader

## 📋 Очередь (готовые команды)

### application-skills (membranedev) → app-коннекторы
3000+ скиллов под аппки (gmail/slack/hubspot…). **Прогнать SkillSpector перед install** (не верифицированы NVIDIA).
**⚠️ Membrane-брокер:** учётки идут через сторонний сервис — оценивать по каждому чувствительному коннектору.
```bash
npx skills add membranedev/application-skills/<app>   # cherry-pick конкретный коннектор
```

### Jina Reader → остальные data-репо (тот же приём)
Eclipse-webclaw / business-data-platform-mvp / Lead-Sniper / Open Researcher — `r.jina.ai/<url>` как fallback-экстрактор где сейчас raw HTML.

## 🔑 Нужен GPU

### ml-intern (HuggingFace) → ModelForge
Автономный ML-инженер (smolagents, Doom-Loop detector). Реальное обучение требует **H100-класс GPU**.
```bash
git clone https://github.com/huggingface/ml-intern
# либо Claude Code skill-порт: https://github.com/AlexWortega/claude-ml-intern-skill
```
План (когда будет GPU): автономный fine-tuning loop под small-модели ModelForge.

## ⏸ Не внедрено
- **Учебник по ИИ-агентам (Google Drive PDF)** — неверифицируемый источник (возможен копирайт/подмена). Не качаем; предпочитаем верифицируемые источники из библиотеки (Build Your Own OpenClaw, Codex Use-Cases).

## Следующие шаги
1. Pavel: смержить PR #4 (Jina RAG) после зелёного CI.
2. Решить по ml-intern (нужен GPU) и application-skills (Membrane-доверие).
3. star-crm — по отдельному одобрению.
