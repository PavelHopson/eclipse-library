# Установки 28.05–05.06.2026 — план внедрения по проектам

> Снапшот внедрения находок недельного батча (28.05–05.06) во все репозитории
> `E:\projects`. Статусы: ✅ сделано (в PR) · 📋 очередь (additive, готовые команды) ·
> 🔑 нужен ключ/инфра от Pavel · 🖐 ручное действие Pavel · ⏸ ждёт одобрения.
>
> **Дисциплина:** правки исходников идут через feature-branch + PR (CI typecheck,
> т.к. локально npm недоступен). Additive-клоны скиллов — в `.claude/`.
> **`star-crm` не трогаем** до явного одобрения (отдельный приватный продакшн).

---

## ✅ Сделано в эту сессию (в PR)

### 1️⃣ eclipse-library — батч 28.05–05.06 + 3 промпт-файла
**PR:** [PavelHopson/eclipse-library#3](https://github.com/PavelHopson/eclipse-library/pull/3) (ветка `feat/curation-week4` → `master`)
- Новый раздел подборки (~33 находки, провенанс верифицирован веб-поиском)
- `prompts/skeptical-verifier.md`, `prompts/feedback-protocols-6.md`, `prompts/resume-toolkit-7.md`
- Grey/high-risk раздел (по запросу владельца, дисклеймеры сохранены)
- Fix: ранее untracked `lyra-prompt-optimizer.md` + `lists/web-utilities.md`

### 2️⃣ eclipse-ai-hub — Copywriter: Resume Toolkit + тон «Скептик»
**PR:** [PavelHopson/eclipse-ai-hub#2](https://github.com/PavelHopson/eclipse-ai-hub/pull/2) (ветка `feat/copywriter-resume-skeptic`)
- **Где:** `src/pages/Copywriter.tsx`
- Шаблон `{ id: 'resume', label: '💼 Резюме / Найм' }` + `RESUME_PRESETS` (7 пресетов: аудит → ATS → результаты → summary → адаптация → сопроводительное → STAR-симулятор)
- Тон `{ id: 'skeptic', label: '🧐 Скептик' }` + `SKEPTIC_SYSTEM_PROMPT` (анти-sycophancy)
- Паттерн — как существующие `PRESENTATION_PRESETS` / `HUMANIZE_PRESETS`
- **Источники:** `prompts/resume-toolkit-7.md`, `prompts/skeptical-verifier.md`
- ⏳ Дождаться зелёного CI на PR перед merge

---

## 🖐 Ручное действие Pavel — глобально (один раз, влияет на все репо)

### Security Guidance (Anthropic, официальный, бесплатный)
Pre-tool hook ловит 25 vuln-паттернов при Write/Edit во **всех** Claude Code сессиях.
```bash
# В любой Claude Code сессии:
/plugin marketplace add anthropics/claude-code
/plugin install security-guidance@anthropics
# Дальше работает автоматически как pre-tool hook — команд не нужно
```
Docs: https://code.claude.com/docs/en/security-guidance

### Codex for OSS (OpenAI) — ChatGPT Pro + Codex на ~полгода бесплатно
Заявка на самый звёздный публичный репо (кандидаты: `eclipse-ai-hub`, `eclipse-chat`, `oh-my-claudecode`).
Форма: https://openai.com/form/codex-for-oss/ · указать репо, звёзды/загрузки, как ИИ поможет.

---

## 📋 Очередь — additive-установки (готовые команды, низкий риск)

### NVIDIA SkillSpector → eclipse-ai-hub, eclipse-hopson-sentinel, oh-my-claudecode
Сканер-gate для любых скиллов (64 паттерна / 16 категорий) перед подключением community-каталогов.
```bash
git clone --depth 1 https://github.com/NVIDIA/SkillSpector
# Запуск по целевой папке скиллов:
python -m skillspector scan ./.claude/skills
```
Применять как gate перед install скиллов из 754-кибербез / 1700+ openclaw каталогов.

### AgentShield → oh-my-claudecode (+ репо с `.claude/`)
Аудит `.claude/` на prompt injection / config drift (102 правила). Есть GitHub Action.
```bash
# CLI:
npx @affaan-m/agentshield scan ./.claude
# или добавить как CI-шаг (GitHub Action) на репо с .claude/ конфигами
```
Репо: https://github.com/affaan-m/agentshield

### NVIDIA/skills (cherry-pick) → eclipse-ai-hub Security · eclipse-hopson-sentinel
Каталог 110 верифицированных скиллов. **Не bulk-vendor** — брать точечно, проверяя SkillSpector'ом.
```bash
# просмотр каталога:
git clone --depth 1 https://github.com/NVIDIA/skills
```

### ECC skills (cherry-pick) → oh-my-claudecode
Брать отдельные скиллы/сабагенты, **не весь конфиг** (конфликт команд/хуков с OMC).
Репо: https://github.com/affaan-m/ECC

### Claude Code мега-стек — точечно под задачу
- `musistudio/claude-code-router` → когда нужен не-Anthropic бэкенд
- `anthropics/claude-code-action` → CI/PR-автоматизация в репозиториях
- `smtg-ai/claude-squad` → параллельные агенты (git worktrees)
- `davila7/claude-code-templates` → bootstrap новых проектов

---

## 🔑 Требует ключа / инфраструктуры от Pavel (фичи, не one-liner)

### GPT-SoVITS (RVC-Boss) → eclipse-hopson-sentinel (TTS/PTT) · eclipse-chat (voice) · Educator-AI · eclipse-dnd-forge (NPC)
Сильнейший RU-клон голоса. **Нужен:** GPU-хост для inference + решение по деплою (локальный сервис / отдельный воркер).
```bash
git clone https://github.com/RVC-Boss/GPT-SoVITS   # upstream, НЕ перезалив fuckSelf
# веса с HuggingFace в GPT_SoVITS/pretrained_models; WebUI для теста
```
План: завернуть в HTTP-эндпоинт → backend job-queue (как уже описано в mapping для LongCat/Removerized).

### Reve 2.0 → shotforge · Text2Image · eclipse-ai-hub Image Studio · (позже star-crm карточки)
Layout-based генерация/правка картинок (#2 на Text-to-Image Arena). **Нужен:** API-ключ Reve.
- Closed SaaS, ~$0.0067/img, app.reve.com → API. Интеграция opt-in где важна правка зон/текста.

### MisoTTS 8B (опц.) → англоязычные озвучки
Эмотивный TTS, 110 мс. Только EN. **Нужен:** GPU. На радар, не приоритет (для RU — GPT-SoVITS).

### agentmemory (оценить) → eclipse-hopson-sentinel · oh-my-claudecode
Persistent-память. **Решение Pavel:** не дублирует ли текущую file-based память + MemOS.
```bash
npm install -g @agentmemory/agentmemory && agentmemory connect claude-code
```
Гипербола «−95% токенов» — не верить, мерить реальный лифт.

---

## 🎯 Прочие точечные внедрения (по репозиториям)

| Репозиторий | Что внедрить | Тип |
|---|---|---|
| **eclipse-chat** | Spatial audio (WebAudio `PannerNode`/HRTF поверх Socket.io) · GPT-SoVITS TTS-сообщения | фича 🔑 |
| **eclipse-dnd-forge** | GPT-SoVITS/MisoTTS голоса NPC · Reve портреты · awesome-video-prompts сцены | фича 🔑 |
| **InterviewForge** | Resume Toolkit 7 промптов (идеальный фит) · MisoTTS голос mock-интервью | фича |
| **Educator-AI** | TTS озвучка квестов · Skeptical Verifier факт-чек | фича 🔑 |
| **Smart-Fitness-Booking-Agent** | GPT-SoVITS RU-голос · Feedback-протоколы | фича 🔑 |
| **CryptoPulse** | Skeptical Verifier (запрет overconfident-прогнозов) · Crawl4AI парсер новостей | промпт/парсер |
| **shotforge / Text2Image** | Reve 2.0 (layout-edit) · awesome-video-prompts (img→video) | фича 🔑 |
| **Lead-Sniper-CAT-Analytics** | OSINT (⚠️ только открытые источники, без «пробива») · Crawl4AI/Firecrawl | парсер |
| **business-data-platform-mvp** | Crawl4AI/Firecrawl LLM-ready ingestion · Security Guidance | парсер |
| **Eclipse-Claw / Eclipse-webclaw** | CloakBrowser антибот-бэкенд (свои авторизованные сессии) | фича |
| **Лендинги** (zefir-gift, dark_roast, RentKld, EclipseForgeLanding, PremiumRent) | Reve hero/продуктовые визуалы · transitions.dev микроанимации · browserytools оптимизация ассетов | ассеты |
| **бэкенды** (KmlApiApp, AdService, wireguard-bot, task-manager, retry-http) | Security Guidance авто-vuln-скан (главные бенефициары) | глобально 🖐 |
| **eclipse-library** | TrendShift для регулярного пополнения · SkillSpector перед листингом скиллов | процесс |
| **PavelHopson (profile)** | Витрина для заявки Codex for OSS | 🖐 |

---

## ⏸ star-crm — ТОЛЬКО после одобрения Pavel

> По прямому указанию: в последнюю очередь и с явным одобрением. Не трогать без go-ahead.

Потенциал (когда дадут добро): Reve 2.0 (карточки товара — правка зон/текста) ·
Skeptical Verifier (качество AutoReply) · CloakBrowser/Crawlee (антибот WB/Ozon на
авторизованных сессиях продавца) · Security Guidance / AgentShield в build-pipeline.

---

## Следующие шаги
1. Pavel: смержить PR #3 (library) и PR #2 (AI Hub) после зелёного CI.
2. Pavel: глобально поставить Security Guidance + подать Codex for OSS.
3. Решить по 🔑-фичам (Reve API-ключ, GPU-хост для GPT-SoVITS) — после этого делаю интеграции по очереди.
4. star-crm — по отдельному одобрению.
