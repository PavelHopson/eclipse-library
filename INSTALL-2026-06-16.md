# Установки 10–16.06.2026 — план внедрения по проектам

> Снапшот: что из батча 09.06(вечер)–16.06 куда внедряем. Статусы: 🎯 high-value ·
> 📋 очередь · 📚 reference · ⏸ не внедряем. **`star-crm` / `starmarket` не трогаем** до одобрения.
> Дубли (SkillSpector, Kimi WebBridge, ml-intern, application-skills, Jina, учебник, handwriting)
> уже разобраны в прошлых батчах — здесь только новое.

## 🎯 Сделать в первую очередь

### Claude Max для OSS — подать заявку (дедлайн 30.06)
6 мес Claude Max (~$1200) за публичный репо с активностью (коммиты/релизы/ревью за 3 мес).
- **Кандидаты-репо PavelHopson** (публичные, активные): `eclipse-ai-hub`, `eclipse-hopson-sentinel`,
  `CryptoPulse`, `Eclipse-Claw`, `eclipse-chat`. Выбрать самый звёздный/живой.
- Подача: <https://claude.com/contact-sales/claude-for-oss> — описать пользу проекта.
- **Решение за Pavel:** на какой репо подаём.

### Ponytail → oh-my-claudecode + личный global skill
Skill «минимальный код» — экономия токенов на всех репо. Ставить рано в цепочку скиллов.
```bash
npx skills add DietrichGebert/ponytail        # или вручную в ~/.claude/skills/
```
- Прогнать через **SkillSpector** перед install (как все community-скиллы — см. прошлый батч).
- В `oh-my-claudecode` — сравнить с текущими skill'ами на «лаконичность», не дублировать.

## 📋 Очередь (по репо)

### lottie skill → Shotforge · Text2Image · EclipseForgeLanding
Генерация Lottie из промпта/SVG.
```bash
npx skills add diffusionstudio/lottie
```
- **Shotforge / Text2Image:** анимированные превью/ассеты.
- **EclipseForgeLanding:** лоадеры, micro-interactions, живой логотип (фронт уже на VPS).

### CodeWhale → ai-setup (опция локального агента)
«Claude Code для DeepSeek/Ollama». Reference-цель для `ai-setup` (генерация конфигов).
```bash
npm install -g codewhale
```
- Оценить персоны/память/суб-агенты vs наш стек; не replace, а альтернативный локальный путь.

### supergoal → oh-my-claudecode (cherry-pick)
Roadmap-оркестрация (`/supergoal` → фазы+файлы состояния, `/goal` → исполнение+аудит).
- **Пересекается с OMC** — взять идею файлов плана/состояния/контекста точечно, не весь конфиг.

### Kimi Work → CryptoPulse (data) + reference
- **CryptoPulse:** встроенные Yahoo Finance + World Bank без API — оценить как источник данных.
- **Hopson Sentinel / oh-my-claudecode:** browser-control (WebBridge уже листан) + swarm — reference.

### Промпты → Eclipse AI Hub / InterviewForge
- `prompts/finance-15.md` → пресет-персона «Финансовый стратег» (Hub Chat/Copywriter).
- `prompts/resume-boost-3.md` (+ `resume-toolkit-7.md`) → InterviewForge career-копилот.

### ImageToPrompt → Shotforge / Text2Image (своя фича)
Reverse-prompt «повторить стиль картинки». Расширение closed → реализовать **свою** через
Gemini Vision (картинка → структурный промпт), не тащить чужой сервис.

### Pi-hole → VPS Mara (опц.)
Сетевой адблок/анти-телеметрия на сервере. Docker одной командой; держать на отдельном порту,
не конфликтуя с Caddy (80/443). Опционально — низкий приоритет.

## 📚 Reference / обучение (в библиотеке, код не внедряем)
- **Claude Fable 5 prompting guide** (official) — свериться при ревизии system-пресетов.
- **Hands-On-AI-Engineering** (50+ гайдов) — паттерны RAG/agents/voice/OCR для Hub/Sentinel.
- **Kaggle 5-day AI Agents** — интенсив (старт 15.06), для прокачки.
- **Fable 5 system prompt (leak)** — reference структуры system-промпта (провенанс под вопросом).
- **Notion Opus 4.8 / Stack AI / GLM 5.2** — бесплатный доступ к моделям для разовых задач.

## ⏸ Не внедряем (grey / high-risk / hype)
- **Gemma 4 Obliterated**, **LocallyUncensored** — uncensored, только reference, вне продукта.
- **flac.music.hi.cn** — пиратство (для «скачать своё» есть Eclipse Media).
- **MiMo Code / GLM 5.2 «> Fable 5»** — маркетинговый хайп, держим на радаре, не эндорсим.
- **«Fable 5 торрент 3.4ТБ»** — фейк (сам автор признал шутку).
- **Hidden-city / mistake fares** (travel-промпты) — нарушают ToS авиакомпаний, только личный риск.
- **OpenAI $50k credits** — приватность: данные идут в обучение; только некритичные эксперименты.

## Следующие шаги
1. **Pavel:** выбрать репо для заявки Claude Max OSS (до 30.06).
2. Ponytail + lottie — прогнать SkillSpector → внедрить (skills, низкий риск).
3. CodeWhale / supergoal / Kimi Work — оценить точечно по репо.
4. `star-crm` / `starmarket` — по отдельному одобрению.
