(() => { const printer = document.querySelector('.printer'); const status = document.querySelector('.status span'); const start = document.querySelector('#start'); const reset = document.querySelector('#reset'); let timer; function setStage(stage) { printer.dataset.stage = stage; status.textContent = stage === 'processing' ? 'Заказ обрабатывается' : stage === 'printing' ? 'Печатаем ваш чек' : 'Заказ выполнен'; } start.addEventListener('click', () => { clearTimeout(timer); setStage('printing'); start.disabled = true; timer = setTimeout(() => { setStage('complete'); start.disabled = false; }, matchMedia('(prefers-reduced-motion: reduce)').matches ? 50 : 1850); }); reset.addEventListener('click', () => { clearTimeout(timer); setStage('processing'); start.disabled = false; }); })();
