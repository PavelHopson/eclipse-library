# Source motion collection

This directory contains 24 ready-made motion studies imported from the user's local folder
`E:\ADMIN_HOPSON_PC\Рабочий стол\Анимации` on 2026-08-26.

- The original folder was not modified.
- Original source files are preserved where practical; local compatibility files use the
  `.local.*` suffix.
- Runtime network dependencies were removed. The gallery loads every demo from the same origin
  inside an iframe sandboxed with `allow-scripts` only.
- `creative-login` uses a pinned local copy of GSAP 3.13.0. Its checksum and provenance are
  recorded in `vendor/NOTICE.md`.
- Licensing information was not present for every item in the supplied folder. These studies
  should therefore be treated as internal references until provenance is confirmed per item.
